<form method="post" action="{{ path('app_approvisionnement_delete', {'id': approvisionnement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ approvisionnement.id) }}">
    <button class="btn btn-danger m-3">Supprimer</button>
</form>
